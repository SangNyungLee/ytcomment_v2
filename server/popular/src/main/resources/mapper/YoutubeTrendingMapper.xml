<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="site.ytcomment.popular.mapper.TrendingMapper">
    <insert id="insertVideo">
        INSERT INTO yttest (
                            id, channelTitle, title
            ,description, thumbnails, channelId, tags, categoryId, publishedAt
        )
        VALUES (#{id}, #{channelTitle}, #{title}, #{description}, #{thumbnails}, #{channelId}, #{tags}, #{categoryId}, #{publishedAt})
    </insert>
</mapper>